# 2026-07-23

`/c/TDT/Synapse/Tanks/cnn_gp_mep_all_udp_v2-260626-115952/torpedo/preprocessed/rRew03-260626-130955/raw_data_mep.parquet` is an example of a timeseries created based on data from `/c/TDT/Synapse/Tanks/cnn_gp_mep_all_udp_v2-260626-115952/rRew03-260626-130955`. `/c/TDT/Synapse/Tanks/cnn_gp_mep_all_udp_v2-260626-115952/torpedo/preprocessed/rRew03-260626-130955/stim_info_per_pulse.parquet`, on the other hand, is a processed 'events' type stream created from the same data. I'd like to enable tdt-explore to ingest these types of data, in addition to the raw tdt-native types it already ingests.

You can explore the two example files for the structure of the parquets, which are saved pandas dataframes; it's ok to add dependencies, if, e.g. we don't already have pandas or the requisite parquet dependency; they are created by the companion repository tss-pipeline, which can be found at `C:\Users\MBO\Documents\GitHub\tss-pipeline`

Two discovery mechanisms: first, by default, scan '<tank-dir>/torpedo/preprocessed/<dataset-dir>` for parquet files. Here, we need to figure out how to distinguish timeseries from events, and how to attribute a sample rate to the timeseries. Check the index of the parquet, I believe it is in units of TDT clock ticks, which corresponds to the 24414.0625 Hz sample rate, or decimated versions of that sample rate. second mechanism: buttons at the bottom of the GUI for 'add processed time series' and 'add processed events', which launch file system prompts for the user to specify one or more parquets from arbitrary locations.